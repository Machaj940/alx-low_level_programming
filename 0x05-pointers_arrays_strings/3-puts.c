#include "holberton.h"
/**
 * _puts - prints a string followed by a newline
 * @s: a string
 * Return: str
 */
void _puts(char *str)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
		_putchar(s[i]);
	}
	_putchar('\n');
}
