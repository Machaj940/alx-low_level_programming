#include "holberton.h"
/**
 * _abs - computes the absolute value of an integer
 * @int: any number
 *
 * Return: Always 0
 */
int print_last_digit(int)
{
	int n;
	int j = n % 10;

	if (n > 0 || n < 0 || n == 0)
	{
		putchar(j);
	}
}
