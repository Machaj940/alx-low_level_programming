#include "holberton.h"
/**
 * _abs - computes the absolute value of an integer
 * @int: any number
 * Return: Always 0
 */
int _abs(int)
{
	int n;
	int j = (-1) * n;

	if (n < 0)
	{
		putchar(j);
	}
}
